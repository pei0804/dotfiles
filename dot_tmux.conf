# ============================================================
# 基本設定
# ============================================================

# Prefix: C-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# True color & 256 color
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",ghostty:Tc,xterm-256color:Tc"

# 拡張キー (Shift+Enter等をtmux経由で送るために必要)
set -sa terminal-features ",xterm-256color:extkeys"
set -g extended-keys on

# マウス操作
set -g mouse on

# ウィンドウ/ペイン番号を1から開始
set -g base-index 1
setw -g pane-base-index 1

# ウィンドウ番号の自動振り直し
set -g renumber-windows on

# エスケープキーの遅延を除去
set -sg escape-time 0

# ヒストリ上限
set -g history-limit 50000

# ウィンドウタイトルを自動更新
set -g set-titles on
set -g set-titles-string "#S:#I #W"

# フォーカスイベントを有効化
set -g focus-events on

# シェルの PATH を tmux 環境に引き継ぐ (display-popup 等で必要)
run 'tmux set-environment -g PATH "$PATH"'

# ============================================================
# キーバインド
# ============================================================

# reload config
bind r source-file ~/.tmux.conf \; display-message "Config reloaded"

# --- ペイン分割 ---
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# --- ペイン移動 (hjkl) ---
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# --- 新規ウィンドウ (カレントディレクトリ) ---
bind c new-window -c "#{pane_current_path}"

# --- fzfセッション選択 (既存選択 / 新規作成) ---
bind s display-popup -E '~/.local/bin/tmux-sessionizer'

# --- Shift+Enter (Kitty keyboard protocol) ---
# tmux経由でShift+Enterが効かない問題の対策
# Claude Code等の複数行入力に必要
bind -n S-Enter send-keys Escape "[13;2u"

# ============================================================
# 通知 (Claude Code入力待ち対応)
# ============================================================

# ベル検知 → ステータスバーでウィンドウをハイライト
set -g monitor-bell on
set -g visual-bell off
set -g bell-action other

# アクティビティ監視
setw -g monitor-activity on
set -g visual-activity off

# ============================================================
# Catppuccin テーマ (TPMプラグイン)
# ============================================================

set -g @catppuccin_flavor "mocha"

set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_default_text " #W"
set -g @catppuccin_window_current_text " #W"

set -g status-left ""
set -g status-right "#{E:@catppuccin_status_session}"
set -g status-right-length 100

# ============================================================
# TPM (Tmux Plugin Manager)
# ============================================================

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'wfxr/tmux-fzf-url'

# TPM初期化 (この行は末尾に配置)
run '~/.tmux/plugins/tpm/tpm'
